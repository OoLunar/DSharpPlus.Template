version: "3.9"

services:
  DSharpPlusTemplateLower:
    build: .
    #image: ghcr.io/oolunar_dsharpplus_template:latest
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./logs:/src/logs
      - ./res/config.json:/src/config.json